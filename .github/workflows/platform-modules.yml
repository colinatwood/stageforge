name: StageForge Platform Modules

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  platform-smoke:
    name: ${{ matrix.name }} platform smoke
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: Linux
            artifact: stageforge-module-linux
          - os: windows-latest
            name: Windows x64
            artifact: stageforge-module-windows
          - os: macos-14
            name: macOS Apple Silicon
            artifact: stageforge-module-macos
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Run exact platform module smoke
        shell: bash
        env:
          STAGEFORGE_CI_EVIDENCE: platform-module-smoke.json
        run: python ci/platform_snapshot_smoke.py
      - name: Run Windows SCM lifecycle smoke
        if: runner.os == 'Windows'
        shell: bash
        run: python ci/windows_service_smoke.py --evidence windows-service-smoke.json
      - name: Run native launch-binding smoke
        if: runner.os == 'Windows' || runner.os == 'macOS'
        shell: bash
        env:
          STAGEFORGE_LAUNCH_EVIDENCE: platform-launch-binding-smoke.json
        run: python ci/platform_launch_binding_smoke.py
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            platform-module-smoke.json
            windows-service-smoke.json
            platform-launch-binding-smoke.json
          if-no-files-found: error
          retention-days: 30
